import React, { FC } from 'react'

import { Layout } from '../../components/common/Layout'

const index: FC = () => {
  return (
    <Layout>
      <h2>部隊スキル一覧</h2>
    </Layout>
  );
}

export default index
