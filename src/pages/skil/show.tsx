import React, { FC } from 'react'

import { Layout } from '../../components/common/Layout'

export const config = { amp: true }

const skil: FC = () => {
  return (
    <Layout>
      <h2>スキル詳細</h2>
      <p>要らないかもね</p>
    </Layout>
  );
}

export default skil
